// import { ISquareShapePosition } from "../types";
// import { CaptchaSquareBoxSize, VideoCamDefaultHeight, VideoCamDefaultWidth } from "../utils/constValues";

// export const startInterval = (setSquareShapePosition: React.Dispatch<React.SetStateAction<ISquareShapePosition>>) => {
//   return setInterval(() => {
//     setSquareShapePosition({
//       x: Math.floor(Math.random() * (VideoCamDefaultWidth - CaptchaSquareBoxSize)),
//       y: Math.floor(Math.random() * (VideoCamDefaultHeight - CaptchaSquareBoxSize)),
//     });
//   }, 1500);
// };
