const SuccessShowing = ({ title }: { title: string }) => {
  return (
    <section className="flex items-center justify-center h-screen bg-[#03285D]">
      <p>{title}</p>
    </section>
  );
};

export default SuccessShowing;
